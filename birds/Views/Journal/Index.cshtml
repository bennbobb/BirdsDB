@model IEnumerable<birds.Models.Journal>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.JournalName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JournalDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JournalName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JournalDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.JournalID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.JournalID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.JournalID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
